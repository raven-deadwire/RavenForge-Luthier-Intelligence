</span><span class="arrow">→</span><span class="node">r = d/L</span></div>`)}<p class="refs">${x.fig1}</p><h2>${x.s2}</h2><p>${x.p2}</p>${V('HTML DIAGRAM',`<div class="row"><span class="node">Geometry<br>r=d/L</span><span class="arrow">→</span><span class="node">Mode<br>sin(nπr)</span><span class="arrow">→</span><span class="node">Weight<br>|sin(nπr)|</span></div>`)}${P('RESEARCH PLOT · calculated / not measured','heatmap')}<h2>${x.s3}</h2><p>${x.p3}</p>${V('HTML DIAGRAM',`<div class="row"><span class="node">Pluck q/L</span><span class="arrow">×</span><span class="node">Pickup d/L</span><span class="arrow">→</span><span class="node">Magnetic transfer</span><span class="arrow">→</span><span class="node">Fixed load</span><span class="arrow">→</span><span class="node">Modal spectrum</span></div>`)}<h2>${x.s4}</h2><p>${x.p4}</p>${V('HTML DIAGRAM',`<div class="row"><span class="node">Point sensor<br>g(u)=δ(u)</span><span class="arrow">→</span><span class="node">Finite kernel<br>g(u)</span><span class="arrow">→</span><span class="node">Spatial average</span></div>`)}${P('RESEARCH PLOT · illustrative aperture ratios','aperture')}<h2>${x.s5}</h2><p>${x.p5}</p>${V('HTML DIAGRAM',`<div class="row"><span class="node">Fret F<br>L_F ↓</span><span class="arrow">→</span><span class="node">r_F=d/L_F ↑</span><span class="arrow">+</span><span class="node">Per string<br>rᵢ=dᵢ/Lᵢ</span></div>`)}<h2>${x.s6}</h2><p>${x.p6}</p>${V('HTML DIAGRAM',`<div class="flow"><span class="node">Raw WAV</span><span class="arrow">→</span><span class="node">Onset + window</span><span class="arrow">→</span><span class="node">Local peak search</span><span class="arrow">→</span><span class="node">Amplitude / phase</span></div>`)}${P('RESEARCH PLOT · illustrative spectrum','spectrum')}<h2>${x.s7}</h2><p>${x.p7}</p>${V('HTML DIAGRAM',`<div class="flow"><span class="node">Geometry</span><span class="arrow">→</span><span class="node">Stabilize</span><span class="arrow">→</span><span class="node">Repeatable pluck</span><span class="arrow">→</span><span class="node">Specified load</span><span class="arrow">→</span><span class="node">Record</span><span class="arrow">→</span><span class="node">Modal tracking</span><span class="arrow">→</span><span class="node">Compare</span><span class="arrow">→</span><span class="node">Uncertainty</span></div>`)}<h2>${x.s8}</h2><p>${x.p8}</p><h2>${x.s9}</h2><table><tr><th>Claim</th><th>Verdict</th></tr>${myths}</table><h2>${x.s10}</h2><p>${x.p10}</p><div class="callout refs">${x.refs}</div>`;heat();ap();sp();document.querySelectorAll('[data-l]').forEach(b=>b.onclick=()=>{history.replaceState(null,'','?lang='+b.dataset.l);render(b.dataset.l)})}let lang=new URLSearchParams(location.search).get('lang')||'ko';render(lang);
</script></main></body></html>