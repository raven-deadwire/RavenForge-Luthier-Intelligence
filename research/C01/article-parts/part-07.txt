</span><span class="arrow">→</span><span class="node">r = d/L</span></div>`)}<p class="refs">${x.fig1}</p><h2>${x.s2}</h2><p>${x.p2}</p>${V(lang==='ko'?'공간 가중 구조':lang==='de'?'Räumliches Gewichtungsmodell':'Spatial weighting model',`<div class="row"><span class="node">Geometry<br>r=d/L</span><span class="arrow">→</span><span class="node">Mode<br>sin(nπr)</span><span class="arrow">→</span><span class="node">Weight<br>|sin(nπr)|</span></div>`)}${P(lang==='ko'?'이상적 공간 가중치 계산 · 실측 데이터 아님':lang==='de'?'Berechnete räumliche Gewichtung · keine Messdaten':'Ideal spatial-weighting calculation · not measured data','heatmap')}<h2>${x.s3}</h2><p>${x.p3}</p>${V(lang==='ko'?'Pluck–pickup 신호 경로':lang==='de'?'Anschlag–Pickup-Signalweg':'Pluck–pickup signal path',`<div class="row"><span class="node">Pluck q/L</span><span class="arrow">×</span><span class="node">Pickup d/L</span><span class="arrow">→</span><span class="node">Magnetic transfer</span><span class="arrow">→</span><span class="node">Fixed load</span><span class="arrow">→</span><span class="node">Modal spectrum</span></div>`)}<h2>${x.s4}</h2><p>${x.p4}</p>${V(lang==='ko'?'유한 감지 폭 모델':lang==='de'?'Modell der endlichen Abtastbreite':'Finite sensing-aperture model',`<div class="row"><span class="node">Point sensor<br>g(u)=δ(u)</span><span class="arrow">→</span><span class="node">Finite kernel<br>g(u)</span><span class="arrow">→</span><span class="node">Spatial average</span></div>`)}${P(lang==='ko'?'유한 감지 폭 응답 · 예시 비율':lang==='de'?'Antwort endlicher Abtastbreite · Beispielverhältnisse':'Finite-aperture response · illustrative ratios','aperture')}<h2>${x.s5}</h2><p>${x.p5}</p>${V(lang==='ko'?'프렛·멀티스케일 좌표 변환':lang==='de'?'Bund- und Multiscale-Koordinaten':'Fret and multiscale coordinate migration',`<div class="row"><span class="node">Fret F<br>L_F ↓</span><span class="arrow">→</span><span class="node">r_F=d/L_F ↑</span><span class="arrow">+</span><span class="node">Per string<br>rᵢ=dᵢ/Lᵢ</span></div>`)}<h2>${x.s6}</h2><p>${x.p6}</p>${V(lang==='ko'?'스펙트럼 분석 절차':lang==='de'?'Ablauf der Spektralanalyse':'Spectral-analysis workflow',`<div class="flow"><span class="node">Raw WAV</span><span class="arrow">→</span><span class="node">Onset + window</span><span class="arrow">→</span><span class="node">Local peak search</span><span class="arrow">→</span><span class="node">Amplitude / phase</span></div>`)}${P(lang==='ko'?'FFT bin과 modal peak 비교 · 예시 스펙트럼':lang==='de'?'FFT-Bin vs. lokaler Modenpeak · Beispielspektrum':'FFT bin vs local modal peak · illustrative spectrum','spectrum')}<h2>${x.s7}</h2><p>${x.p7}</p>${V(lang==='ko'?'C01 측정 워크플로':lang==='de'?'C01-Messablauf':'C01 measurement workflow',`<div class="flow"><span class="node">Geometry</span><span class="arrow">→</span><span class="node">Stabilize</span><span class="arrow">→</span><span class="node">Repeatable pluck</span><span class="arrow">→</span><span class="node">Specified load</span><span class="arrow">→</span><span class="node">Record</span><span class="arrow">→</span><span class="node">Modal tracking</span><span class="arrow">→</span><span class="node">Compare</span><span class="arrow">→</span><span class="node">Uncertainty</span></div>`)}<h2>${x.s8}</h2><p>${x.p8}</p><h2>${x.s9}</h2><table><tr><th>Claim</th><th>Verdict</th></tr>${myths}</table><h2>${x.s10}</h2><p>${x.p10}</p><div class="callout refs">${x.refs}</div>`;heat();ap();sp();document.querySelectorAll('[data-l]').forEach(b=>b.onclick=()=>{history.replaceState(null,'','?lang='+b.dataset.l);render(b.dataset.l)})}let lang=new URLSearchParams(location.search).get('lang')||'ko';render(lang);
</script></main></body></html>