G — C01</p></div><div class="langs"><button data-l="ko">KR</button><button data-l="en">EN</button><button data-l="de">DE</button></div></div><h1 id="title"></h1><p class="sub" id="sub"></p><div class="purpose" id="purpose"></div><section id="content"></section><script>
const T={"ko":{"lang":"ko","title":"C01 — Pickup Position FFT Mapping","sub":"픽업 위치의 공간 샘플링 가설을 반복 가능한 스펙트럼 측정과 불확도 분석으로 검증하는 방법","purpose":"A02에서 정리한 pickup-position spatial sampling을 실제 측정 프로토콜로 전환한다. C01은 sin(nπd/L)를 완성된 pickup output으로 취급하지 않고, excitation·finite sensing aperture·magnetic transduction·string non-ideality·electrical load를 분리해 위치 signature가 실제 modal spectrum에서 반복되는지 검증한다.","s1":"1. 연구 질문과 측정 경계","p1":"이상적인 fixed-end string에서 point sensor의 공간 위치계수는 Hpos(n)=sin(nπd/L)이다. 이는 geometric spatial factor이며 실제 pickup output 전체가 아니다. 실제 출력에는 pluck point, sensing aperture, magnetic field, electrical transfer와 time-dependent decay가 함께 개입한다.","fig1":"Figure 1. Bridge 기준 d, speaking length L, normalized coordinate r=d/L. Multiscale/slanted pickup은 현마다 dᵢ와 Lᵢ를 기록한다.","s2":"2. 이상적 spatial weighting과 node","p2":"Ideal node condition은 r=m/n이다. 다만 real pickup의 finite magnetic aperture와 비대칭 field 때문에 실제 출력이 완전한 0이 된다고 단정하지 않는다. 아래 도표는 |sin(nπr)|의 계산값이며 RavenForge 실측값이 아니다.","s3":"3. Pluck point와 pickup point는 분리한다","p3":"Ideal triangular-displacement pluck에서는 excitation coefficient가 pluck coordinate q에 의존한다. linearized velocity-sensitive pickup 근사에서는 Vₙ ∝ sin(nπq/L)·sin(nπd/L)/n 형태가 baseline이 된다. 따라서 pickup 위치만 시험하려면 pluck coordinate와 release condition을 고정해야 한다.","s4":"4. Finite sensing aperture","p4":"일반적인 sensitivity kernel g(u)에 대해 H_g(n)=∫g(u)sin[nπ(d+u)/L]du로 쓸 수 있다. 대칭 rectangular kernel에서는 고차 mode에 sinc형 attenuation이 생기지만 중앙 ideal node가 자동으로 이동하지는 않는다. Physical housing/pole width를 effective sensing width와 동일시하지 않는다.","s5":"5. Fretting과 multiscale","p5":"F fret에서 L_F=L₀·2^(−F/12), r_F=d/L_F로 normalized coordinate가 이동한다. Multiscale에서는 각 현마다 rᵢ=dᵢ/Lᵢ를 계산한다. Pickup slant는 dᵢ를 바꿀 뿐 scale 차이를 자동 보정하지 않는다.","s6":"6. Spectral analysis audit","p6":"Exact n·f₀ FFT bin을 그대로 읽는 대신 실제 modal peak를 추적한다. Leakage, finite record, inharmonicity 때문에 peak가 bin center와 다를 수 있다. Zero-padding은 interpolation density를 높일 뿐 physical frequency resolution을 늘리지 않는다.","s7":"7. C01 bench protocol","p7":"C01-A는 가능하면 동일 pickup을 이동시켜 position effect만 분리한다. C01-B는 동일 pickup에서 fret을 바꾸며 r_F migration을 확인한다. A04의 electrical load는 C01에서 연구변수가 아니라 고정 boundary condition으로 기록한다.","s8":"8. Uncertainty와 반복성","p8":"반복 횟수나 ±dB 합격선을 임의로 고정하지 않는다. pilot variance를 먼저 구하고 geometry, pluck coordinate, pickup gap, tuning/speaking length, electrical load/gain, onset/window placement, spectral estimator를 uncertainty budget에 포함한다.","s9":"9. Myth audit","myths":[["Bridge pickup은 항상 더 많은 고조파를 낸다","Needs correction"],["sin(