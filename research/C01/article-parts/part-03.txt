arized velocity-sensitive pickup approximation, Vₙ ∝ sin(nπq/L)·sin(nπd/L)/n is a useful baseline. Therefore plucking coordinate and release conditions must be controlled when testing pickup position.","s4":"4. Finite sensing aperture","p4":"For a general sensitivity kernel g(u), H_g(n)=∫g(u)sin[nπ(d+u)/L]du. A symmetric rectangular kernel produces sinc-like attenuation of higher modes but does not automatically move the central ideal node. Physical housing or pole width must not be equated with effective sensing width.","s5":"5. Fretting and multiscale","p5":"At fret F, L_F=L₀·2^(−F/12) and r_F=d/L_F, so the normalized coordinate migrates. For multiscale instruments calculate rᵢ=dᵢ/Lᵢ for every string. Pickup slant changes dᵢ; it does not automatically compensate for scale-length differences.","s6":"6. Spectral-analysis audit","p6":"Do not read only the exact n·f₀ FFT bin. Track the actual local modal peak because leakage, finite records and inharmonicity can move the peak away from the bin center. Zero-padding increases interpolation density but not the physical resolving power of the observation window.","s7":"7. C01 bench protocol","p7":"C01-A should, where possible, move the same pickup so position is the primary changed variable. C01-B keeps the pickup fixed and changes fret to test r_F migration. The A04 electrical load is recorded as a fixed boundary condition rather than a C01 research variable.","s8":"8. Uncertainty and repeatability","p8":"Do not impose an arbitrary repeat count or ±dB acceptance limit before a pilot. Estimate pilot variance first and include geometry, pluck coordinate, pickup gap, tuning/speaking length, electrical load/gain, onset/window placement and spectral estimator in the uncertainty budget.","s9":"9. Myth audit","myths":[["Bridge pickup always produces more harmonics","Needs correction"],["sin(nπd/L) is the complete pickup output","Ideal-model only"],["An ideal node guarantees zero real output","Not guaranteed"],["Housing width equals sensing width","Unsupported"],["Finite width always moves the node","Incorrect for symmetric baseline"],["Fretting leaves weighting unchanged","Incorrect"],["Multiscale pickup slant automatically compensates scale","Unsupported"],["Reading exact n·f₀ bins is sufficient","Incorrect"],["Zero-padding raises true frequency resolution","Incorrect"],["96 kHz is always more accurate","Unsupported as universal claim"],["Hₙ/H₁ is always sufficient normalization","Conditional"],["One pluck is enough for validation","Insufficient"],["Mohamad 3.53 mm is a manufacturing tolerance","Incorrect use of evidence"]],"s10":"10. Current RavenForge design position","p10":"Record pickup placement as a relationship between per-string/per-fret normalized coordinate and measured modal response, n